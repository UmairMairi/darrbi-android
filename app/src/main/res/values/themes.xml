<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Window/launch theme only. Real UI styling comes from the Compose design system. -->
    <style name="Theme.Darrbi" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <!-- Black launch background so the branded splash shows no flash (logo is light-on-dark). -->
    <style name="Theme.Darrbi.Splash" parent="Theme.Darrbi">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
